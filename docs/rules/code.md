# 코드 규칙

- 관리하는 소스 파일은 주석과 빈 줄을 포함해 50줄 이하로 유지.
- 한 줄 압축이나 세미콜론 나열로 줄 수 제한을 피하는 방식 금지.
- 함수와 모듈에는 하나의 명확한 역할만 부여.
- 사용자가 실행하는 파일에는 시작 코드만 두고 실제 기능은 하위 모듈에 배치.
- Windows와 Mac에서 함께 쓰는 기능은 `scripts/core`에 배치.
- 공개 명령 변경 후에도 기존 자동화 명령 유지.
- 경로, 종료 코드, 문자 인코딩 동작은 재발 방지 테스트로 보호.
