# 코드 규칙

- 관리하는 소스 파일은 주석과 빈 줄을 포함해 50줄 이하로 유지한다.
- 한 줄 압축이나 세미콜론 나열로 제한을 우회하지 않는다.
- 함수와 모듈은 하나의 명확한 책임만 갖는다.
- 사용자가 실행하는 파일에는 시작 코드만 두고 실제 기능은 하위 모듈에 둔다.
- Windows와 Mac에서 함께 쓰는 기능은 `scripts/core`에 둔다.
- 공개 명령을 바꿔도 기존 자동화 명령이 계속 작동하게 한다.
- 경로, 종료 코드, 인코딩 동작은 회귀 테스트로 보호한다.
