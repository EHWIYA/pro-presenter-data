# Git 규칙

- 이미 구성된 저장소에서 `git init`과 `remote add`를 반복하지 않는다.
- 작업 전 상태를 확인하고 사용자의 기존 변경을 보존한다.
- 자동 커밋 대상은 ProPresenter Git 자산으로 제한한다.
- 변경이 없으면 빈 커밋을 만들지 않는다.
- 커밋은 한 문장으로 설명 가능한 단위로 나눈다.
- 강제 초기화나 광범위한 삭제는 명시적 승인 없이 실행하지 않는다.
- 완료 시 테스트 결과, 커밋, 남은 기존 변경을 보고한다.
